Inicializar após o back-end

Como inicializar o projeto:

- Acesse o diretório do projeto e, no terminal, execute o comando "npm start" e, caso seja necessário, digite "Y" para aceitar alteração de porta de rede.

- Para executar os testes unitários, executar "yarn test"